{
  "scripts": {
    "build": "esbuild game.js --bundle > build.js",
    "serve": "npx serve"
  },
  "dependencies": {
    "-": "^0.0.1",
    "esbuild": "^0.13.4",
    "g": "^2.0.1",
    "kaboom": "^2000.0.0-beta.24",
    "npm": "^7.24.2"
  }
}
